# Watti

Menu bar app that shows power/wattage details on macOS.

## Local build

```bash
./build.sh
open build/Watti.app
```

## GitHub Releases (macOS)

Push a tag like `v1.0.0` and GitHub Actions will attach `Watti-macos.zip` to the release.

