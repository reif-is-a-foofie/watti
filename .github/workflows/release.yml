name: Release (macOS)

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build app
        run: ./build.sh

      - name: Ad-hoc sign app (no certificate)
        run: |
          codesign --force --deep --sign - "build/Watti.app"

      - name: Zip, tar.gz, and dmg
        run: |
          ditto -c -k --sequesterRsrc --keepParent "build/Watti.app" "Watti-macos.zip"
          tar czvf Watti-macos.tar.gz -C build Watti.app
          ./scripts/make_dmg.sh
          mv build/Watti.dmg Watti.dmg

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            Watti-macos.zip
            Watti-macos.tar.gz
            Watti.dmg

